{
  "port": 18801,
  "profile": "runtime",
  "credentialsPath": "~/.claude/.credentials.json",
  "compatibilitySets": ["openclaw"],
  "routing": {
    "type": "profile"
  },
  "profiles": {
    "runtime": {}
  },

  "stripSystemConfig": true,
  "stripToolDescriptions": true,
  "injectCCStubs": false,

  "_comment_layers": "All v2 layers enabled by default. Set to false to disable individual layers.",
  "_comment_sets": "Use compatibilitySets to enable built-in runtime rules. Available sets: openclaw, hermes. Use [] to disable built-in sets and rely only on custom arrays.",
  "_comment_routing": "For multiple subscriptions, set routing.type to clientToken and map local bearer/x-api-key tokens to profile names.",
  "_comment_config": "The replacements, reverseMap, toolRenames, and propRenames arrays are optional custom additions. They merge on top of enabled compatibility sets.",

  "_comment_toolRenames": "Tool name renames are applied as quoted replacements throughout the entire body. The defaults cover known legacy runtime tools. Add entries for any custom tools your runtime adds.",
  "_comment_propRenames": "Property renames handle runtime-specific schema properties. Add entries for any custom properties.",
  "_comment_runtime_template": "See config.runtime.example.json for a sanitized multi-runtime compatibility template copied from the current local deployment."
}
